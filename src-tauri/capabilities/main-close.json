{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-close",
  "description": "Complete the main window close after pending conversation and draft writes finish",
  "windows": ["main"],
  "permissions": ["core:window:allow-destroy"]
}
